# SecureLock
This project implements a Bluetooth-based smart door lock system using an Arduino Uno. The system allows a user to enter a password via a mobile application, which is transmitted wirelessly using a Bluetooth module. Based on password verification,a servo motor controls the locking and unlocking mechanism, and LCD provides real-time system feedback.
